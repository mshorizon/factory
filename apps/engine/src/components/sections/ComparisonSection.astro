---
import type { Section } from "@mshorizon/schema";
import { ComparisonTable, SectionHeader } from "@mshorizon/ui";
import { getSectionHeader } from "../../lib/pages";

interface Props {
  isHomePage?: boolean;
  section: Section;
  badgeVariant?: string;
}

const { section, isHomePage = false, badgeVariant } = Astro.props;
const header = getSectionHeader(section);
const rows = (section as any).rows || [];
const leftTitle = (section as any).leftTitle || "Manual Work";
const rightTitle = (section as any).rightTitle || "AI Automation";
---

<section class={`pb-${isHomePage ? "spacing-section" : "spacing-section-sm"}`}>
  <div class="container mx-auto">
    <SectionHeader
      badge={header.badge}
      title={header.title}
      subtitle={header.subtitle}
      align="center"
      background={section.background}
      badgeVariant={badgeVariant as any}
    />
    <ComparisonTable
      client:visible
      leftTitle={leftTitle}
      rightTitle={rightTitle}
      rows={rows}
    />
  </div>
</section>
