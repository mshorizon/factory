---
import type { Section } from "@mshorizon/schema";
import { HeroDefault, HeroSplit, HeroGradient, HeroCards, HeroVideo, HeroMinimal } from "@mshorizon/ui";
import { getSectionVariant, getSectionHeader } from "../../lib/pages";
import { resolveTarget } from "../../lib/target";

interface Props {
  section: Section;
}

const { section } = Astro.props;
const variant = getSectionVariant(section);
const header = getSectionHeader(section);
const heroImage = section.image || section.backgroundImage;

// Resolve CTA targets to hrefs
const cta = section.cta ? {
  label: section.cta.label,
  href: section.cta.href || resolveTarget(section.cta.target),
  variant: section.cta.variant,
} : undefined;

const secondaryCta = section.secondaryCta ? {
  label: section.secondaryCta.label,
  href: section.secondaryCta.href || resolveTarget(section.secondaryCta.target),
  variant: section.secondaryCta.variant,
} : undefined;
---

{variant === "split" ? (
  <HeroSplit
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    image={heroImage}
    cta={cta}
    secondaryCta={secondaryCta}
    testimonial={section.testimonial}
    testimonials={section.testimonials}
  />
) : variant === "gradient" ? (
  <HeroGradient
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    backgroundImage={section.backgroundImage}
    cta={cta}
    secondaryCta={secondaryCta}
  />
) : variant === "cards" ? (
  <HeroCards
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    image={heroImage}
    cta={cta}
    secondaryCta={secondaryCta}
  />
) : variant === "video" ? (
  <HeroVideo
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    backgroundImage={section.backgroundImage}
    cta={cta}
    secondaryCta={secondaryCta}
  />
) : variant === "minimal" ? (
  <HeroMinimal
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    image={heroImage}
    cta={cta}
    secondaryCta={secondaryCta}
  />
) : (
  <HeroDefault
    client:visible
    badge={header.badge}
    title={header.title || ""}
    subtitle={header.subtitle}
    backgroundImage={section.backgroundImage}
    cta={cta}
    secondaryCta={secondaryCta}
    fullHeight
  />
)}
