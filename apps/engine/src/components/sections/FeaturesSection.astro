---
import type { Section } from "@mshorizon/schema";
import { FeaturesGrid, FeaturesCompact, FeaturesGradient, FeaturesIconTiles, SectionHeader } from "@mshorizon/ui";
import { getSectionVariant, getSectionHeader } from "../../lib/pages";

interface Props {
  isHomePage?: boolean;
  section: Section;
  badgeVariant?: string;
}

const { section, isHomePage = false, badgeVariant } = Astro.props;
const variant = getSectionVariant(section);
const header = getSectionHeader(section);
import { resolveTarget } from "../../lib/target";

const rawItems = section.items || [];
const items = rawItems.map((item: any) => ({
  ...item,
  linkLabel: item.linkLabel,
  linkHref: item.linkHref || item.href || (item.linkTarget ? resolveTarget(item.linkTarget) : undefined),
}));
---

<section class={`pb-${isHomePage ? "spacing-section" : "spacing-section-sm"}`}>
  <div class="container mx-auto">
    <SectionHeader
      badge={header.badge}
      title={header.title}
      subtitle={header.subtitle}
      background={section.background}
      badgeVariant={badgeVariant as any}
    />
    {variant === "compact" ? (
      <FeaturesCompact client:visible items={items} />
    ) : variant === "gradient" ? (
      <FeaturesGradient client:visible items={items} />
    ) : variant === "icon-tiles" ? (
      <FeaturesIconTiles client:visible items={items} />
    ) : (
      <FeaturesGrid client:visible items={items} />
    )}
  </div>
</section>
