---
import type { Section } from "@mshorizon/schema";
import { TestimonialsGrid, SectionHeader } from "@mshorizon/ui";
import { getSectionHeader } from "../../lib/pages";

interface Props {
  isHomePage?: boolean;
  section: Section;
}

const { section, isHomePage = false } = Astro.props;
const header = getSectionHeader(section);
const items = section.items || [];
---

<section class={`py-${isHomePage ? "spacing-section" : "spacing-section-sm"}`}>
  <div class="container mx-auto">
    <SectionHeader
      badge={header.badge}
      title={header.title}
      subtitle={header.subtitle}
      layout="split"
      align="left"
      background={section.background}
    />
    <TestimonialsGrid client:visible items={items} />
  </div>
</section>
