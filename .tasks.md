task: food ordering review + redesign (template-restaurant / nostrano)

- [x] Review of the ordering feature — findings: no i18n (mixed EN/PL), SSR hydration errors on every cart page, accept-order hard-required Stripe, no payment methods, weak add-to-cart feedback, generic cart/checkout design
- [x] Add-to-cart UX — new floating mini-cart bar (bottom of viewport, mobile + desktop, independent of auto-hiding navbar): item count, total, "Zobacz zamówienie" → /checkout
- [x] Cart redesign — merged into /checkout ("Twoje zamówienie"): editable items (qty, remove, edit customizations) + form + summary in one view, serif headings, theme tokens only
- [x] Merge cart+checkout — /cart now 301-redirects to /checkout
- [x] Payment methods — `payments.paymentMethods` in schema (`online` | `cash` | `card_on_site`), selector at checkout, `payment_method` column on orders, accept flow branches (offline orders skip Stripe + skip "paid" stage), admin OrdersTab aware
- [x] Order status page — prominent pulsing "Czekamy na potwierdzenie restauracji" hero, placed-at time, auto-refresh note, call-restaurant button, connected progress tramline, payment-method-aware stages, full i18n
- [x] DB migration applied (`orders.payment_method`, default 'online') via `pnpm db:push`
- [x] E2E verified locally on template-restaurant: cash order → accept (no Stripe) → ETA → preparing (status page auto-updates, no "Opłacone" stage); online order → accept → Stripe test session + "Zapłać" button on status page. Test orders cleaned up.
- [ ] Deploy: `git pull` + `pm2 restart astro-dev` on VPS for *.dev.hazelgrouse.pl; prod auto-deploys on push
