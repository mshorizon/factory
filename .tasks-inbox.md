# Task Inbox

Add new tasks here while the runner is active — safe to edit anytime, no conflicts.
The runner merges `* [ ]` lines into `.tasks.md` at each poll cycle, then clears them here.

### Admin Panel
* [ ] Admin panel > (1) Left sidebar should be whole while in lightMode. (2) Center panel should have grey background but cards should be white.
* [x] Admin panel > (1) Every tab component now have very little width - i want that is should be 100% available width

