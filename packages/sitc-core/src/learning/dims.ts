/**
 * Embedding dimension — must match `sitc_lessons.embedding` in the DB schema
 * (@mshorizon/db SITC_EMBED_DIM). Adjust together when the embedding model changes.
 */
export const SITC_EMBED_DIM = 1536;
