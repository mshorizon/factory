* for business template "portfolio-tech":
  * modify the first page name in navbar to "Home" ("Główna" in polish)
  * move a little bit to top content of hero section
  * add particle effect in hero section: white pixcels apear and move(animate) to the center
  * "Our Process" section:
    * steps should be centered but gap should be still 1.25rem
    * instead of images - there should be videos - download and save in claudflare
  * "Case Studies" section:
    * max carusell width should be 1000px and should be stored as variable. the same variable should be used to set width item in carusell
    * for items in carusell:
      * above item title there is image/component as first letter of title. relace this component using logos with texts from component: "50+ local businesses already online"
  * "Benefits" section
    * should use the same badge and header type as other sections - best fit is `Case Studies` section (new badge version + centered texts)
    * every kard should have more intense purple gradient
  * "Pricing" section:
    * cards and buttons should have less visible borders
  * "FAQ" section:
    * should be centered as `Case Studies` section (centered texts)
    * questions should have max 780px width or around - use spacing from theme
    * questions should be centered too
    * borders of questions should be less visible
  * Footer:
    * probably there is wrong footer selected - we had footer with top gradient and icon - now there is lack of those elements

