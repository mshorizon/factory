"self-improving template creator"

I want to add a feature to my project (including a repo) that, based on a given website (the first parameter of the tool is the website URL, most often a landing page), creates a JSON file in the database or repo (the repo will be the final one). This is the business JSON file that forms the website rendering engine. I already have many variants of sections and atoms. Now I want to add a mechanism to this project where I would provide a link to another website, and the code would browse that page, then iteratively copy the design of that page using existing variants, modify them, or create new ones. I'd like this mechanism to be loop-based: browsing the website design, implementing it to bring our code closer to the target page, checking if it's close. If it's not, it starts over. The code could run for hours, but its goal is to get closer to the target design. Each section would be evaluated, because with a single generation, some sections could improve, while others could deteriorate. It's probably worth continuing only with the improved ones. How to do it - describe in the file?